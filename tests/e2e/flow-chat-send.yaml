# Test: Chat Send Flow
# Suite: regression
# Platform: web
# Tags: chat, send, flow, critical

url: http://localhost:4567
---
- assertVisible:
    selector: "textarea[placeholder='Type a message...']"
- inputText:
    selector: "textarea[placeholder='Type a message...']"
    text: "Hello Agora!"
- tapOn:
    text: "Send"
- assertNotVisible:
    text: "Review a file"
- takeScreenshot: "chat-send"
